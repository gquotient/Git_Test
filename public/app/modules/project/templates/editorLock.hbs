{{#if isLocked }}
{{#if isEditable }}
<button type="button" class="button primary release">{{$ 'Release Lock' }}</button>
{{else}}
{{#if hasEditor }}
<div class="lock-message">
{{ editor }} {{$ 'has this project locked' }}
</div>
{{/if}}
{{/if}}
{{else}}
<button type="button" class="button primary edit">{{$ 'Edit Model' }}</button>
{{/if}}
