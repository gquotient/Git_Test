// Playing with google web fonts
//@import url("http://fonts.googleapis.com/css?family=News+Cycle");

// Stylus includes
@import 'shared';
@import 'nib';
@import 'nib/gradients';

/* ==========================================================================
   Misc Tweaks (sourced from https://github.com/h5bp)
   ========================================================================== */

/*
 * Remove text-shadow in selection highlight: h5bp.com/i
 * These selection declarations have to be separate.
 * Customize the background color to match your design.
 */

::-moz-selection {
  background: #b3d4fc;
  text-shadow: none;
}

::selection {
  background: #b3d4fc;
  text-shadow: none;
}

/*
 * A better looking default horizontal rule
 */

hr {
  display: block;
  height: 1px;
  border: 0;
  border-top: 1px solid #ccc;
  margin: 1em 0;
  padding: 0;
}

/*
 * Remove the gap between images and the bottom of their containers: h5bp.com/i/440
 */

img {
  vertical-align: middle;
}

/*
 * Remove default fieldset styles.
 */

fieldset {
  border: 0;
  margin: 0;
  padding: 0;
}

/*
 * Allow only vertical resizing of textareas.
 */

textarea {
  resize: vertical;
}

/* ==========================================================================
   Core Styles
   ========================================================================== */

* {
  -webkit-box-sizing: border-box; /* Safari/Chrome, other WebKit */
  -moz-box-sizing: border-box;  /* Firefox, other Gecko */
  box-sizing: border-box;     /* Opera/IE 8+ */
}

html {
  height: 100%;
}

body {
  background: colorBGDarkGrey;
  color: colorPrimaryText;
  font-size: 85%; /* Resets 1em to 12px */
  line-height: 1.25em;
  font-family: Helvetica, Arial, Sans-Serif;
  font-weight: 100;
  border-top: 4px solid colorDrakerOrange;
  height: 100%;
}

h1, h2, h3 {
  padding:0;
  margin: 0 0 12px;
  font-weight: normal;
}

h4 {
  padding: 0;
  margin: 0 0 6px;
  font-size: 1em;
  font-weight: bold;
}

/* Messages */

.message {
  border: 1px solid #aaa;
  background: #ccc;
  box-shadow: inset 0 1px 0 #ddd;
  color: #111;
  padding: 3px;
  border-radius: 1px;
  &.flash {

  }
  &.error {
    background: #FFC1B8;
    border-color: colorAlertRed;
  }
}

/* Buttons and Links */

button.button, a.button {
  background: #ddd;
  background-image: linear-gradient(#ddd, #bbb)
  display: inline-block;
  line-height: 22px;
  min-height: 22px; /* FireFox doesn't like line-height in its buttons */
  padding: 0 .75em;
  margin: 0 6px 0 0;
  font-size: 1em;
  font-weight: normal;
  text-decoration: none;
  cursor: pointer;
  border-radius: 3px;
  text-align: center;
  border: 1px solid #aaa;
  color: #555;
  text-shadow: 0 1px 0 #eee;
  box-shadow: inset 0 1px 0 #eee;
  &.primary {
    background-color: colorDrakerOrange;
    background-image: linear-gradient(#f57a2a, #f0501c);
    border: 1px solid colorDrakerOrange;
    color: #fff;
    text-shadow: 0 1px 0 #d84515;
    box-shadow: inset 0 1px 0 #f8ad7c;
    &.sml {
      color: #fff;
    }
  }
  &.disabled,
  &.disabled:hover {
    border: 1px solid #ccc;
    background-color: #eee;
    background-position: 0 -600px;
    color: #999;
    text-shadow: 0 1px 0 #fff;
  }
  &.sml {
    line-height: 16px;
    min-height: 16px; /* FireFox doesn't like line-height in its buttons */
    padding: 0 .75em;
    margin: 0 3px 0 0;
    font-size: .75em;
    vertical-align: text-bottom;
    border-radius: 2px;
    color: #333;
  }
}

td .button {
  line-height: 16px;
  min-height: 16px; /* FireFox doesn't like line-height in its buttons */
  padding: 0 .75em;
  margin: 0 3px 0 0;
  font-size: .75em;
  vertical-align: text-bottom;
  border-radius: 2px;
}

a, a:active, a:hover, a:visited {
  color: colorDrakerOrange;
}

.buttons.container {
  margin: 0 0 12px;
}

/* Tables */

table.basic {
  background: colorBGDarkGrey;
  margin: 0 0 12px;
  padding: 0;
  border: borderBasicDark;
  thead {
    height: 24px;
    background: colorBGDarkGrey;
    box-shadow: 0 3px 3px rgba(0, 0, 0, 0.1);
    background-image: linear-gradient(colorSoftGradientGreyStart, colorSoftGradientGreyStop);
    border-top: borderBasicMedium;
    border-bottom: borderBasicDark;
    tr {
      th {
        line-height: 24px;
        padding: 0 6px;
        font-weight: bold;
        text-align: left;
        text-shadow: 0 1px 0 #222;
        &:first-child {
          //border-left: none;
        }
        &:last-child {
          //border-right: none;
        }
      }
    }
  }
  tbody {
    color: #aaa;
    tr {
      &:nth-child(odd) {
        background-color: colorBGMediumGrey;
      }
      &:hover {
        background-color: colorBGLightGrey;
      }
      &:last-child {
        border-bottom: none;
      }
      td {
        padding: 6px 6px;
        &.label {
          font-weight: bold;
        }
      }
    }
  }
  input[type="checkbox"],
  input[type="radio"] {
    padding: 0;
    margin: 0;
    vertical-align: middle;
  }

  input:disabled {
    border: none;
    background: none;
    color: colorPrimaryText;
  }
}

td.status {
  width: 24px;
}

td.name {
  white-space: nowrap;
}

.statusIndicator {
  background: colorOKBlue;
  width: 12px;
  height: 12px;
  display: block;
  text-indent: -9999px;
  overflow: hidden;
  border-radius: 6px;
  box-shadow: 0 1px 0 #444;
  float: left;
  //margin: 0 auto;
  &.Warning {
    background: colorWarningYellow;
  }
  &.Alert {
    background: colorAlertRed;
  }
}


/* Forms */

form {
  margin: 0;
  padding: 0;
  ul {
    padding:0;
    margin:0 0 12px;
    overflow:hidden;
    li {
      padding:0;
      margin:0 0 .75em;
      &:last-child {
        padding:0;
        margin:0;
      }
      label {
        padding:0;
        margin:0;
        display:block;
      }
    }
  }
}

input[type="email"], input[type="password"], input[type="text"], input[type="password"], textarea {
  font-family:Helvetica, sans-serif;
  background:#fff;
  border:1px solid #111;
  line-height:20px;
  padding:3px;
}

input[type="checkbox"], input[type="radio"] {
  padding:0;
  margin:0;
  vertical-align: middle;
}

select[disabled], select:disabled {
  color: #555;
}

/* Misc Layout */

leftColumnWidth = 240px;
leftColumnWidthSml = 200px;

.column_left {
  width: leftColumnWidth;
  float: left;
  margin-left: -100%;
  height: 100%;
  overflow: auto;
  @media screen and (max-width: 1024px) {
    width: leftColumnWidthSml;
  }
}

.column_right {
  position: relative;
  margin-left: leftColumnWidth;
  transition: 0.25s ease-in-out;
  @media screen and (max-width: 1024px) {
    margin-left: leftColumnWidthSml;
  }
  height: 100%;
  overflow: auto;
}

.column_right_wrapper {
  height: 100%;
  width: 100%;
  float: left;
}

.verticalPane {
  height: 50%;
  overflow: auto;
  border-bottom: primaryBorderStyle;
  position: relative;
}

.container {
  overflow: hidden;
  &.half {
    float: left;
    width: 50%;
  }
  &.module {
    overflow: hidden;
    padding:6px;
    height: 300px;
    &.half {
      float: left;
      width: 50%;
    }
    @media screen and (max-width: 1000px) {
      &.half {
        float: none;
        width: 100%;
      }
    }
  }
}

.dropdown {
  position: relative;
  &:hover {
    background-color: colorDropDownBG;
    ul {
      display: inherit;
    }
  }
  &#pageSettings {
    float:right;
  }
  ul {
    display: none;
    position: absolute;
    right: 0;
    top: 28px;
    padding: 0;
    margin: 0;
    z-index: 301;
    white-space: nowrap;
    background: colorDropDownBG;
    text-shadow: none;
    list-style-type: none;
    li {
      font-size: .85em;
      line-height: 24px;
      padding: 0 6px;
      border-bottom: 1px solid #404040;
      a {
        display: inline-block;
        width: 100%;
        line-height: 24px;
        color: #ccc;
        text-decoration: none;
        &:hover {
          color: #fff;
        }
      }
    }
  }
}

/* Baseline leaflet styles */

.leaflet-container {
  width: 100%;
  height: 100%;
  background: #B5D0D0 url(loadingIcon) center center no-repeat;
  // Map View Icons
  .leaflet-marker-icon {
    background-position: 0 0;
    background-repeat: no-repeat;
    width: 15px;
    height: 32px;
    &.ok {
      background-image: url(../img/marker_ok.png);
    }
    &.warning {
      background-image: url(../img/marker_warning.png);
    }
    &.alert {
      background-image: url(../img/marker_alert.png);
    }
  }
  .leaflet-popup {
    color: #555;
    .leaflet-popup-content-wrapper {
      h4 {
        padding:0;
        margin: 0 0 6px;
      }
      p {
        padding: 0;
        margin: 0 0 3px;
      }
      a {
        color: colorDrakerOrange;
      }
      border-radius: 2px!important;
    }
  }
  .leaflet-control {
    transition:.2s;
    opacity: 0.6;
    box-shadow: 0 3px 3px rgba(0,0,0,0.1);
    border: 1px solid #aaa;
    margin: 6px;
    &:hover {
      opacity: 1;
      transition:.2s;
    }
  }
  .layerControls {
    position: absolute;
    top: 0;
    right: 0;
    margin: 9px;
    padding: 6px;
    border: 1px solid #aaa;
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.8);
    box-shadow: 0 3px 3px rgba(0,0,0,0.1);
    color: #555;
    ul {
      padding: 0;
      margin: 0;
      list-style-type: none;
      li {
        padding: 0;
        margin: 0 0 3px;
        &:last-child {
          margin: 0;
        }
      }
    }
  }
  button.fullscreen {
    position: absolute;
    bottom: 0;
    left: 0;
    margin: 6px;
  }
}


/* Charts */

.chart {
  height: 300px;
  width: 100%;
}

/* Admin */

.admin-list {
  background: colorBGDarkGrey;
  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    > li {
      padding: 5px;
      text-shadow: 0 1px 0 #222;
      cursor: pointer;
      border-top: borderBasicMedium;
      border-left: borderBasicMedium;
      border-bottom: borderBasicDark;
      border-right: borderBasicDark;
      background: linear-gradient(colorSoftGradientGreyStart, colorSoftGradientGreyStop);
      &:hover {
        background: linear-gradient(#252627, #282828);
      }
    }
  }
}

/* Modal */

.modal {
  position: absolute;
  z-index: 1001;
  height: auto;
  perspective: 800px;
  perspective-origin: 50% 100px;
  &.hidden {
    transform:scale3d(0,0,0);
  }
}

.obscure {
  z-index: 999;
  background: rgba(0,0,0,0.75);
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  min-height: 100px;
  height: 100%;
}

/* Date Selection */

.dateSelection {
  &.multi {
    height: 24px;
    position: relative;
    .current {
      line-height: 22px;
      background: #111;
      color: #777;
      float: left;
      border: 1px solid #555;
      height: 100%;
      border-top-left-radius: 2px;
      border-bottom-left-radius: 2px;
      padding: 0 6px;
      font-size: 12px;
      span {
        color: #ccc;
      }
    }
    .expand {
      width: 24px;
      height: 100%;
      line-height: 22px;
      float: left;
      background: linear-gradient(#555, #444);;
      border: 1px solid #555;
      border-left: none;
      overflow: hidden;
      text-align: center;
      //text-indent: -200%;
      border-top-right-radius: 2px;
      border-bottom-right-radius: 2px;
      cursor: pointer;
      color: #111;
      text-shadow: 0 1px 0 #777;
    }
    .calendar {
      position: absolute;
      border: 1px solid #555;
      top: 23px;
      right: 0;
      background: #111;
      padding: 6px;
      box-shadow: 0 5px 5px rgba(0,0,0,0.1);
      .actions {
        height: 18px;
        button {
          float: right;
        }
      }
    }
  }

  .datepicker {
    a {
      color: #ccc;
    }
    a.datepickerGoNext, a.datepickerGoPrev, a.datepickerMonth {
      height: 20px;
      line-height: 20px;
      float: none;
    }
    a.datepickerMonth {
      text-align: center;
      margin: 0;
      width: 100%;
    }
    .datepickerViewDays {
      > thead {
        > tr {
          th {
            position: relative;
            .datepickerGoPrev {
              position: absolute;
              top: 0;
              left: 0;
            }
            .datepickerGoNext {
              position: absolute;
              top: 0;
              right: 0;
            }
          }
        }
      }
    }
    .datepickerSelected {
      background: colorDrakerOrange;
    }
  }
}

/* Loading Indicator */

@keyframes rotate {
  from {transform: rotate(0deg);}
  to   {transform: rotate(359deg);}
}

.loadingIndicator {
  margin: 6px;
  display: inline-block;
  width: 9px;
  height: 9px;
  border: 1px solid #ccc;
  border-right: none;
  border-radius: 50%;
  animation: rotate 1s linear infinite;
}
